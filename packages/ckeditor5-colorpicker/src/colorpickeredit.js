import Plugin from '@ckeditor/ckeditor5-core/src/plugin';

export default class ColorPickerEditing extends Plugin {
    init() {
        console.log( 'ColorPickerEditing#init() got called' );
    }
}
