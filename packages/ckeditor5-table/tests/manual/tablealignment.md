### Table alignment

All available values of alignment:
- `left` (with text wrapping)
- `right` (with text wrapping)
- `center`  (with no text wrapping)
- `blockLeft` (with no text wrapping)
- `blockRight`  (with no text wrapping)
