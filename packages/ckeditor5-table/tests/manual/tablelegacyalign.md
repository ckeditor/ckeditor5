### Table Legacy Align

This test verifies the behavior of the legacy `align` attribute applied to `<td>` elements. It ensures that table content and columns align correctly when the deprecated `align` attribute (e.g., `align="left"`, `align="center"`, `align="right"`) is present on cells in the source HTML.
