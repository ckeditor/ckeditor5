# List separator feature

This is an editor instance with the `AdjacentListsSupport` plugin enabled. It separates two lists of the same type (`ol` + `ol` or `ul` + `ul`) if they are next to eachother.
