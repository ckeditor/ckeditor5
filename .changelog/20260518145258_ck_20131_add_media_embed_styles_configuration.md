---
type: Feature
scope:
  - ckeditor5-media-embed
closes:
  - https://github.com/ckeditor/ckeditor5/issues/20131
---

Added `config.mediaEmbed.styles.options` for picking a subset of the built-in media embed styles, overriding their fields, or registering custom ones. The `config.mediaEmbed.toolbar` now also accepts inline split-button dropdown definitions.
