---
type: Feature
scope:
  - ckeditor5-table
closes:
  - https://github.com/ckeditor/ckeditor5/issues/12952
communityCredits:
  - star-szr
---

Added support for table footers. It should be now possible to toggle certain table rows as footers and have them preserved in the data with proper HTML comments.

This feature is disabled by default and can be enabled by setting the `table.enableFooters` configuration to `true`.
