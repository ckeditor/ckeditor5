---
type: Fix
scope:
  - ckeditor5-utils
closes:
  - https://github.com/ckeditor/ckeditor5/issues/20238
---

The `EmitterMixinConstructor`, `ObservableMixinConstructor` and `DomEmitterMixinConstructor` types no longer resolve to `undefined` in projects that compile with the `strictNullChecks` option disabled.
