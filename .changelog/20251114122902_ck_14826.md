---
type: Fix
scope:
  - ckeditor5-table
closes:
  - https://github.com/ckeditor/ckeditor5/issues/14826
communityCredits:
  - bendemboski
---

Fixed an issue where table cells with `rowspan` attributes were not properly accounted for when computing `headingColumns` during upcast. This caused header columns to be incorrectly stripped when using `editor.setData()` on tables with merged cells in the heading columns.
