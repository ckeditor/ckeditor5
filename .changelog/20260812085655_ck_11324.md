---
type: Fix
scope:
  - ckeditor5-table
closes:
  - https://github.com/ckeditor/ckeditor5-commercial/issues/11324
---

The `content-table` and `layout-table` classes set on the `<figure>` element wrapping a content table are no longer preserved by the General HTML Support feature as arbitrary classes.
