---
type: Other
scope:
  - ckeditor5-ui
closes:
  - https://github.com/ckeditor/ckeditor5-commercial/issues/10051
---

Improved the `HighlightedTextView` component so that it correctly processes special input, such as text containing HTML-special characters (e.g. `&`, `<`, `>`).
