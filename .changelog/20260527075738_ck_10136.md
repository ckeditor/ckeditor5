---
type: Fix
scope:
  - ckeditor5-media-embed
closes:
  - https://github.com/ckeditor/ckeditor5-commercial/issues/10136
---

Fixed the Spotify track embed using a fixed height instead of a fluid aspect ratio to prevent incorrect resizing.

Spotify track embeds now render at a fixed `80px` height, matching the compact single-row player. Album and artist embeds are unaffected and continue to use a responsive aspect ratio.
