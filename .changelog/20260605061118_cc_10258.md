---
type: Fix
scope:
  - ckeditor5-core
closes:
  - https://github.com/ckeditor/ckeditor5-commercial/issues/10258
---

An invalid tag name passed to `config.root.element` or `config.roots.<rootName>.element` now throws a clear `CKEditorError` with code `editor-wrong-element-name` instead of a cryptic `InvalidCharacterError` from the browser renderer.
