---
type: Fix
scope:
  - ckeditor5-find-and-replace
---

Fixed a memory leak that occurred when replacing all occurrences of a search phrase. Leftover internal data was not cleaned up after the operation, which could accumulate over time and degrade editor performance during long editing sessions.
