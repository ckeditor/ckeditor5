---
type: Fix
scope:
  - ckeditor5-table
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19771
---

Preserved the `<figure>` wrapper on content tables when `table.tableLayout.stripFigureFromContentTable` is set to `false` and the layout tables plugin is enabled.
