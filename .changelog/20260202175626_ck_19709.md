---
type: Fix
scope:
  - ckeditor5-html-support
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19709
---

An inline content (`<img>`) should not be stripped out of `<div>` inside of `<dd>` tag.
