---
type: Fix
scope:
  - ckeditor5-core
---

A cryptic `multi-root-editor-root-initial-data-mismatch` error is no longer thrown when `config.roots` is an object with a custom prototype or a class instance. A dedicated `editor-create-roots-not-plain-object` error with a clear message is now thrown instead.
