---
type: Fix
scope:
  - ckeditor5-engine
  - ckeditor5-undo
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19483
---

Fixed an issue where `RootAttributeOperation` was incorrectly transformed to `NoOperation` when the new value was the same as in the operation it was transformed by. This fixes a bug where undoing root attribute changes would sometimes fail.
