---
type: Fix
scope:
  - ckeditor5-table
closes:
  - https://github.com/ckeditor/ckeditor5/issues/20209
---

Fixed a crash (`conversion-slot-filter-incomplete` error) that occurred when loading or pasting content containing multiple `<table>` elements wrapped in a single aligning `<div>`.
